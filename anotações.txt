na div ano do calendário, talvez mudar cor quando aproximar de um outro ano ao arrastar os dias

vou usar uma api LOCAL de timezone, dps tenho que escolher uma que pegue a timezone do usuário

interatividade (onclick com o site), fazer uma inclusão disto com o SQL

entender o que significa este pycache

entender como adicionar ler as divs em scroll
https://getbootstrap.com/docs/5.3/components/scrollspy/#how-it-works

adicionar eventos?

quais as próximas etapas? destrinchar isso aí

encontrar quantos px tem a scrollspy navbar

fazer uma div que encapsule o mês inteiro para que o spyscroll funcione

nas horas colocar dois inputs hora_min e hora_max e perguntar se quer add mais opções de horas

'''CREATE TABLE "event" (
    "Id_event" INTEGER PRIMARY KEY AUTOINCREMENT,
    "Event" TEXT
    )'''

'''CREATE TABLE "event_days_hours" (
    "Id" INTEGER PRIMARY KEY AUTOINCREMENT,
    "Id_event" INTEGER,
    "Day" TEXT,
    "Month" TEXT,
    "Event_min_hour" INTEGER,
    "Event_min_minute" INTEGER,
    "Event_max_hour" INTEGER,
    "Event_max_minute" INTEGER
    )'''

'''CREATE TABLE "users" (
    "Id_user" INTEGER PRIMARY KEY AUTOINCREMENT,
    "Id_event" INTEGER,
    "Name" TEXT
    )'''

'''CREATE TABLE "users_days_hours" (
    "Id_user" INTEGER,
    "Id_event" INTEGER,
    "Day" TEXT,
    "Month" TEXT,
    "User_min_hour" INTEGER,
    "User_min_minute" INTEGER,
    "User_max_hour" INTEGER,
    "User_max_minute" INTEGER
    )'''


    gerar chave aleatoria para o link url de invite

    adicoinar extended templates?